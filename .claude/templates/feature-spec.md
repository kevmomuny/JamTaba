## Feature: [name]

### User Story
As a [role], I need to [action] so that [outcome].

### Acceptance Criteria (input → output)
GIVEN: [precondition with specific data]
WHEN: [action]
THEN: [observable result — DB state, API response, UI state]

### Constraints (DO NOT)
- [explicit things to NOT build]

### Verification
- [ ] DB: [SQL query that proves correctness]
- [ ] API: [endpoint + expected response]
- [ ] UI: [what user sees — with data values, not just element existence]
